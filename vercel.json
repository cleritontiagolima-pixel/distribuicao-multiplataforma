{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "next build",
  "installCommand": "pnpm install --frozen-lockfile",
  "regions": ["iad1"],
  "crons": [],
  "functions": {
    "api/download/url": { "maxDuration": 60 },
    "api/download/chunk": { "maxDuration": 60 }
  }
}
